#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m;

    cin>>n>>m;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }

        for(int i=0;i<m;i++)
        {
            int idx;
            cin>>idx;
            set <int> elements;

            for(int i=idx;i<=n;i++)
            {
                elements.insert(arr[i]);
            }
            cout<<elements.size()<<endl;
        }


}
